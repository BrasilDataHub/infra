# Perfil CACHE-512MB — o default da imagem e o cenário do Base Empresarial:
# cache + fila (Horizon) de uma aplicação de produção. Limite de container
# recomendado: 1G (headroom p/ fork do AOF rewrite — ver README).
REDIS_MAXMEMORY=512mb
REDIS_MAXMEMORY_POLICY=volatile-lru
