# Perfil CACHE-256MB — projetos pequenos (ex.: Base Escolar): cache de
# aplicação + fila leve. Limite de container recomendado: 512M (headroom
# p/ fork do AOF rewrite — ver README).
REDIS_MAXMEMORY=256mb
REDIS_MAXMEMORY_POLICY=volatile-lru
