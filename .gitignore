.env
.env.metrics
*.local
.DS_Store

# Alvos e segredos do Prometheus são gerados no host pelo setup.sh.
# `targets/README.md` é versionado; o resto do diretório, não.
monitoring/targets/*.json
monitoring/secrets/

# Ficha da infraestrutura: contém IPs, senhas e chaves dos servidores em uso.
# ESTE REPOSITÓRIO É PÚBLICO no GitHub — o arquivo nunca deve ser versionado.
INFRAESTRUTURA.md
