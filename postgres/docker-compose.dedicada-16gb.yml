# Perfil DEDICADA-16gb — máquina exclusiva do Postgres (16 GB RAM, 4 vCPUs,
# SSD/NVMe local). PGDATA em BIND no disco local (/data/pgdata) — nunca
# volume de rede. Porta exposta somente à rede privada (vSwitch/firewall).
# Parâmetros do perfil: env.dedicada-16gb | Guia completo: docs/perfis.md

services:
  postgres:
    image: ghcr.io/brasildatahub/postgres:17
    restart: unless-stopped
    env_file:
      - env.dedicada-16gb
    environment:
      POSTGRES_DB: ${POSTGRES_DB:?defina POSTGRES_DB}
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?defina POSTGRES_PASSWORD}
      DADOS_READ_PASSWORD: ${DADOS_READ_PASSWORD:-}
    volumes:
      - /data/pgdata:/var/lib/postgresql/data
    ports:
      # Exclusivamente o IP privado — nunca 0.0.0.0.
      - "${PRIVATE_IP:?defina PRIVATE_IP}:5432:5432"
    shm_size: 2gb
    deploy:
      resources:
        limits:
          memory: 14G
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
