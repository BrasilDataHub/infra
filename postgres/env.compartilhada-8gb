# Cenário 1 — compartilhada 8 GB (caso atual do baseempresarial: CCX13,
# host dividido com app/Redis/Meilisearch; ~4 GB para o Postgres).
# São os DEFAULTS da imagem — este arquivo existe como fonte de verdade
# explícita do cenário; implantar sem env nenhuma produz o mesmo resultado.
PG_SHARED_BUFFERS=2GB
PG_EFFECTIVE_CACHE_SIZE=4GB
PG_WORK_MEM=32MB
PG_MAINTENANCE_WORK_MEM=512MB
PG_RANDOM_PAGE_COST=1.5
PG_EFFECTIVE_IO_CONCURRENCY=100
PG_JIT=off
PG_MAX_WAL_SIZE=4GB
